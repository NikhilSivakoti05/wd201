{
  "name": "todo-cli",
  "version": "1.0.0",
  "description": "A simple application",
  "main": "todo.js",
  "scripts": {
    "test": "jest",
    "format": "prettier --write '**/*.js'"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "husky": "^8.0.0",
    "prettier": "^3.0.0",
    "eslint": "^8.0.0"
  },
  "husky": {
    "hooks": {
      "pre-commit": "npm test && npm run format"
    }
  }
}
